apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhirhub-frontend.fullname" . }}
  labels:
    {{- include "fhirhub-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "fhirhub-frontend.selectorLabels" . | nindent 4 }}
