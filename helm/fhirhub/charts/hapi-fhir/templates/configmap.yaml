apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hapi-fhir.fullname" . }}-config
  labels:
    {{- include "hapi-fhir.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
